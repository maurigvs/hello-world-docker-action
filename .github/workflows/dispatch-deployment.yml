name: Docker Pipeline

on:
  workflow_dispatch:
    inputs:
      namespace:
        description: namespace
        required: true
        type: string
    secrets:
      USER:
        required: true
      TOKEN:
        required: true

run-name: Build and push > ${{ github.repository }}:${{ inputs.namespace }}

jobs:
  build-push-image:
    name: Build and Push
    uses: ./.github/workflows/build-push-image.yml
    with:
      namespace: ${{ inputs.namespace }}
    secrets: inherit
    permissions:
      contents: read
      packages: write